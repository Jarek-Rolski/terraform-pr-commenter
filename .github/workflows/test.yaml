name: Test

on:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Test Commenter
        run: |
          ./testing/scripts/tests.sh
        shell: bash
